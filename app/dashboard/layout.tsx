/**
 * Dashboard Layout
 * 
 * Layout wrapper for all dashboard pages.
 * This creates the /dashboard route segment.
 */

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
